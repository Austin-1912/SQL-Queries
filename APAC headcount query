-- 1) TXN base restricted to SG/MY, with simplified channel mapping
CREATE OR REPLACE TABLE CAR_905_TXN_BASE AS
SELECT
    t.sndcustomer_key,
    t.sndcountry_code,
    t.txn_id,                           -- adjust column name
    t.rcvpaying_datetime,
   CASE
        WHEN pricing_channel IN ('WEB','APP')       THEN 'DIGITAL'
        WHEN pricing_channel IN ('POS','AIR','TMT') THEN 'RETAIL'
        ELSE 'OTHERS'
    END AS channel_type
FROM WUDNA.SUMMARY_GEN.WUDNA_TXN_MASTER_ANALYTICS_VW t
As dsicussed you will need the total customer base for SG and MY based on all the available transaction data segmented by 

Activity Status - RNT, R12 Active, R24 Active ….. Untill data available period.

Split by Retail, Digital, Omni
any person who have done a transaction in that time period should be considered as Active in that period not take their last txn date. For RNT check if they have a txn in the entire time period 
   CASE WHEN marketing_flag='Y' AND email_address_on_file_flag='Y' AND opt_email='I' THEN 'Y' ELSE 'N' END AS email_marketable_flag,
    CASE WHEN marketing_flag='Y' AND mobile_number_on_file_flag='Y' AND opt_sms='I'   THEN 'Y' ELSE 'N' END AS sms_marketable_flag, for app_marketability , if a customer has done any app txn during that period then they are app marketable 
what we can do is We can do R12 for Jan to Dec 2025, R24 to be Jan to Dec 2024, R36 Jan to Dec 2023, etc and we have data till 2017 
