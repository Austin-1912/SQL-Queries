Need to get the marketability flag for all the customers , we can get email and sms marketable flag through customer master table. include
the below logic in the script
CASE
        WHEN c.marketing_flag             = 'Y'
         AND c.email_address_on_file_flag = 'Y'
         AND c.opt_email                  = 'I' THEN 'Y'
        ELSE 'N'
    END AS email_marketable_flag,
     CASE
         WHEN c.marketing_flag             = 'Y'
          AND c.mobile_number_on_file_flag = 'Y'
          AND c.opt_sms                    = 'I' THEN 'Y'
         ELSE 'N'
    END AS sms_marketable_flag,

CASE WHEN b.email_marketable_flag = 'Y' OR b.sms_marketable_flag = 'Y' THEN 'YES' ELSE 'NO' END AS Marketability_Flag, 
